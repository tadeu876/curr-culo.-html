function toggleSection(id) {
    const section = document.querySelector(`#${id} .details`);
    if (section.style.display === 'none' || section.style.display === '') {
        section.style.display = 'block';
    } else {
        section.style.display = 'none';
    }
}
